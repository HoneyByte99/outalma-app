# QE Report — Outalma — 2026-05-24

**Branch:** `main` @ `4bf84c9` (feat(ui): Build 8 — onboarding flow, auth bg fix, dialog layout, splash fix)
**Reviewer:** QE worker (Pulsar Studio)
**Scope:** pre-release audit before next TestFlight upload.

---

## VERDICT: 🔴 NOT READY

Tests do not compile, `flutter analyze --fatal-warnings` returns errors, and the `pubspec.yaml` build number was not bumped after the last TestFlight upload. The next `flutter build ipa` will either fail CI gating or upload a build that App Store Connect will reject as a duplicate of `1.0.0 (3)`.

---

## 🔴 Blockers

### B1 — Test suite does not compile (28 errors)

`flutter analyze --fatal-warnings` returns **28 errors** in 3 test files. `flutter test` reports `Some tests failed` (3 compile failures, 629 tests still pass).

Failing files:

- `test/application/booking/booking_actions_test.dart` (12 errors)
- `test/application/booking/lifecycle_use_cases_test.dart` (12 errors)
- `test/application/booking/create_booking_use_case_test.dart` (4 errors)

Root cause: the use-case classes were refactored to no-arg `const` constructors that internally instantiate `CallableFunctionClient()`, but the tests still call `AcceptBookingUseCase(functions)` / `CreateBookingUseCase(functions)` / etc. The tests' `_FakeFunctions` injection path is dead.

Example mismatch:

- `lib/src/application/booking/booking_actions.dart:7-14` → `class AcceptBookingUseCase { const AcceptBookingUseCase(); ... }`
- `test/application/booking/booking_actions_test.dart:52` → `AcceptBookingUseCase(functions)`

**Impact:** CI gate "ci: add 25% line coverage threshold gate" (11aaa28) will block any PR; the booking lifecycle (the entire revenue path) is currently untested.

### B2 — `flutter analyze --fatal-warnings` fails

Direct consequence of B1. The release-readiness checklist line `[x] flutter analyze --fatal-warnings — no issues` is **false today** and must be corrected (or fixed) before tagging a release.

### B3 — Build number not bumped

- `pubspec.yaml:5` → `version: 1.0.0+3`
- `docs/release-readiness.md:88` → "IPA uploaded to App Store Connect — build 1.0.0 (3) active"
- Latest commit subject → "Build 8"

The next `flutter build ipa` will produce another `1.0.0+3` IPA. App Store Connect rejects duplicate build numbers. Bump to at least `1.0.0+4` (or `+9` if the commit message is authoritative and 4–8 were used locally) before uploading.

---

## 🟡 Warnings (non-blocking)

### W1 — Stale release-readiness claims

Two checkboxes in `docs/release-readiness.md` are marked complete but no longer hold:

- L51 `[x] flutter analyze --fatal-warnings — no issues` → currently 28 errors.
- L52 `[x] 169 unit/integration tests passing` → 629 currently pass, but 3 test files fail to compile.

These need correction so the readiness doc reflects truth.

### W2 — 34 unchecked items in `docs/release-readiness.md`

Triage:

**Deferrable (post-TestFlight internal):**
- L17 — App Check (already flagged as defer post-TestFlight)
- L73 — Firebase prod project (assumes prod is wired; verify before public TF group)
- L75–77 — Indexes / Storage rules / Functions deployed (verify out-of-band)
- L62–67 — UX polish (design tokens, 3-step flow, dead-end recovery)
- L52–57 — Quality nice-to-haves (90% coverage, no raw maps crossing layers, etc.)

**Should-have before TestFlight (manual smoke list — L34–46):**
- E2E core flows: sign-up → service create → booking → chat → review → push. Not blocking the build, but **must be run manually** on the next IPA before promoting beyond Internal Testers.
- L29 — Firestore rules tested with 2 real accounts (no client-side status writes). Rules look correct (status is locked at `match /bookings`, L128–133) but no recorded multi-account dry-run.
- L28 — PhoneShare unlock window. Rules at L26–30 correctly gate to `["accepted","in_progress","done"]`.
- L23 — `BookingStatus` parity. ✅ Verified during this audit — see "Cross-layer status parity" below.

### W3 — Uncommitted assets and runtime files

`git status` shows:

- `M assets/images/logo_outalma.png` (144 KB, was 187 KB) — image was re-optimized, never committed. Decide whether this belongs in the build (the smaller asset is preferable for binary size) or revert.
- Untracked agent runtime files at workspace root: `.openclaw/`, `.tmp-crash.ips`, `AGENTS.md`, `HEARTBEAT.md`, `IDENTITY.md`, `SOUL.md`, `TOOLS.md`, `USER.md`. These should be added to `.gitignore` before any inadvertent `git add -A` ships them.

---

## ✅ Cross-layer integrity (passes)

### BookingStatus parity — OK

| Layer | Values |
|---|---|
| Dart enum (`lib/src/domain/enums/booking_status.dart`) | `requested`, `accepted`, `inProgress`→`in_progress`, `done`, `rejected`, `cancelled` |
| TypeScript (`functions/src/index.ts:95-101`) | `'requested' \| 'accepted' \| 'rejected' \| 'cancelled' \| 'in_progress' \| 'done'` |
| Firestore rules (`firebase/firestore.rules:29`) | `["accepted", "in_progress", "done"]` (phone-share unlock window) |
| State machine | Dart (`canTransitionTo`) and TS (`acceptBooking`/`markInProgress`/`confirmDone` precondition checks) enforce identical transitions: `requested→{accepted,rejected,cancelled}`, `accepted→in_progress`, `in_progress→done`. |

Serialization round-trip is unit-tested (`test/domain/booking_status_test.dart` — all 10 cases pass).

### Cloud Functions surface — OK

Every callable invoked from Dart exists in `functions/src/`:

| Function called from Dart | Exported in `functions/src/index.ts` |
|---|---|
| `requestPhoneOtp` | ✅ (re-exported from `auth_phone.ts`) |
| `verifyPhoneOtpAndSignIn` | ✅ |
| `verifyPhoneOtpAndSignUp` | ✅ |
| `createBooking` | ✅ |
| `acceptBooking` | ✅ |
| `rejectBooking` | ✅ |
| `cancelBooking` | ✅ |
| `markInProgress` | ✅ |
| `confirmDone` | ✅ |
| `logSession` | ✅ |

Triggers (`onMessageCreate`, `onBookingStatusChange`, `sendBookingReminders`) and admin-only callables (`setAdminClaim`, `setModeratorClaim`, `suspendProvider`, `unsuspendProvider`, `removeService`, `deleteMessage`, `resolveReport`, `revokeUserSessions`) are exported but not called from the app — expected (admin panel or server-driven).

No orphan callables, no missing callables.

---

## Next actions (ordered)

1. **Fix the test compile errors (B1).** Either restore an injectable `FirebaseFunctions` parameter on `AcceptBookingUseCase`/`RejectBookingUseCase`/`MarkInProgressUseCase`/`ConfirmDoneUseCase`/`CancelBookingUseCase`/`CreateBookingUseCase`, or rewrite the test fakes to stub `CallableFunctionClient` instead of `FirebaseFunctions`. Re-run `flutter analyze --fatal-warnings && flutter test` until both are green.
2. **Bump `pubspec.yaml` version (B3)** to `1.0.0+4` (next TF build) before running `flutter build ipa --release`.
3. **Decide on `assets/images/logo_outalma.png`** — commit the smaller version with a `chore(assets): re-optimize logo` commit, or `git restore` it.
4. **Add agent runtime files to `.gitignore`** (`AGENTS.md`, `HEARTBEAT.md`, `IDENTITY.md`, `SOUL.md`, `TOOLS.md`, `USER.md`, `.openclaw/`, `.tmp-crash.ips`).
5. **Correct stale readiness lines** in `docs/release-readiness.md` L51–52 (or fix the underlying issues and re-tick them after CI passes).
6. **Run the manual core-flow smoke list** (release-readiness L34–46) on the new IPA before promoting beyond "Internal Testers".
7. **Multi-account Firestore rules dry-run (L29)** — log evidence in the readiness doc.

---

*Generated by QE worker — branch `main` @ `4bf84c9`, 2026-05-24 00:34 GMT+2.*
