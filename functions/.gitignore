lib/
node_modules/
*.log
.firebase/

# Secrets — never commit
**/service-account*.json
**/*firebase-adminsdk*.json
.env
.env.*
!.env.example
scripts/service-account.json

# Generated by jest --coverage. Never committed: it is a build artefact, it
# dirties the working tree on every test run, and firebase deploy would upload
# it inside the function bundle.
coverage/
